
A = [1 1 1;
     0 1 3;
     0 0 1];


for i = 1:length(x)
    for j = 1:length(y)
        for k = 1:length(
        
        [~,~,relres] = gmres(A,[x(i); y(j)],1);
        r1PlainB(i,j) = relres;
    end
end